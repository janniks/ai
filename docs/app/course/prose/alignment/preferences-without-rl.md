DPO's observation is that the RLHF objective has a known best answer in closed form, and inverting it lets the reward be written in terms of the model itself: how much more probable the model in training makes a response, compared to the frozen reference model it started from. Substituting that into the preference loss gives a single supervised objective. Writing $\pi$ for the model being trained, $\pi_\text{ref}${tip:read this as pi ref, the reference model. It is a frozen copy of the model as it was before this training began, kept unchanged so the training has a fixed baseline to compare against. Measuring probabilities relative to this copy stops the model from drifting too far from what it already knew.} for the reference, $y_w${tip:read this as y with a small w attached, standing for the winning response. It is the answer that the human judge preferred out of the two answers shown for a given prompt.} and $y_l${tip:read this as y with a small l attached, standing for the losing response. It is the answer that the human judge rejected when comparing the two answers for the same prompt.} for the preferred and rejected responses to a prompt $x$, the loss rewards

$$\beta \log \tfrac{\pi(y_w \mid x)}{\pi_\text{ref}(y_w \mid x)} - \beta \log \tfrac{\pi(y_l \mid x)}{\pi_\text{ref}(y_l \mid x)}$${tip:read this as beta times the log of how much more likely the trained model makes the winning answer compared to the frozen reference model, minus the same quantity computed for the losing answer. Making this difference large means raising the probability of the preferred answer and lowering the probability of the rejected one, always measured relative to where the model started. The number beta controls how strongly each comparison pushes the model.}

being large, trained directly on comparison pairs. No reward model, no sampling loop, no PPO. Constitutional AI attacks the other bottleneck, the human labels: a model critiques and revises its own outputs against written principles, and preference data is generated by AI feedback. The two ideas are independent, and modern pipelines mix both freely.
