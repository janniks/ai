Predicting the noise is equivalent, up to rescaling, to estimating the score: at every point, the direction in which data becomes more typical. Seen this way, sampling is following a learned field of arrows from pure randomness to the data, and the step-by-step denoising chain becomes a continuous flow with a fully deterministic counterpart that permits fast samplers.

Flow matching makes the transport view primary: choose any smooth path between noise and data, often the straight line $x_t = (1-t)\,x_0 + t\,x_1${tip:read it as: the point at time t equals one minus t times x-zero, plus t times x-one. Here x-zero is a sample of pure noise, x-one is a real data point, and t is a clock running from zero to one. At time zero you are sitting exactly on the noise, at time one exactly on the data, and in between you slide along the straight line connecting them, which is the path the network learns to travel.}, and train a network to predict the velocity, the direction and speed of travel along that path. Diffusion falls out as one choice of path. The unification matters practically; the field's current samplers, distillation methods, and video models are mostly statements about which path and which solver.
